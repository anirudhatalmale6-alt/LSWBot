var jsonfile = require('jsonfile');

var channels = jsonfile.readFileSync(process.cwd()+"/test.js");

console.log(JSON.stringify(channels));

channels["meow"] = "meow";

jsonfile.writeFileSync(process.cwd()+"/test.js", channels, function(err) { console.log(err); });

channels = jsonfile.readFileSync(process.cwd()+"/test.js");

jsonfile.writeFileSync(process.cwd()+"/test.js", channels, function(err) { console.log(err); });